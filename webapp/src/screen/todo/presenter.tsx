import React from 'react';
import { View, Text } from 'react-native';

interface TodoProps {}

const Todo = (props: TodoProps) => {
  return (
    <View>
      <Text>Todo</Text>
    </View>
  );
};

export default Todo;
